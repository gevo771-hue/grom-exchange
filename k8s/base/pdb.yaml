apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: grom-backend
  namespace: grom
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: grom-backend
  unhealthyPodEvictionPolicy: AlwaysAllow
